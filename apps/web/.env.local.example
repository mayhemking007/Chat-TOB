# NextAuth: must match NEXTAUTH_SECRET in apps/api so API can verify JWTs.
NEXTAUTH_SECRET=

# NextAuth URL (default for local dev).
NEXTAUTH_URL=http://localhost:3000

# OAuth: create apps at https://console.cloud.google.com and https://github.com/settings/developers
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_ID=
GITHUB_SECRET=

# API base URL (used for Credentials and OAuth user sync).
NEXT_PUBLIC_API_URL=http://localhost:4000
