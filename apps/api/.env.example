# PostgreSQL connection string. Example: postgresql://user:password@localhost:5432/chat_tob
# For Neon: use the connection string from your Neon dashboard (pooled recommended).
DATABASE_URL=

# Must match NEXTAUTH_SECRET in apps/web so API can verify JWTs. Leave empty until set.
NEXTAUTH_SECRET=

# Allowed origin for CORS (defaults to http://localhost:3000).
# WEB_ORIGIN=
